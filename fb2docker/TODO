* OSTree /boot labeling
* SELinux policy additions - get to enforcing
  - Ensure "ostree admin upgrade" works
* Publish initial vagrant
* Rename
* Glance format images
  - cloud-init (eww, Python)
* Automate Vagrant provisioning
  - libguestfs, add in ssh keys
